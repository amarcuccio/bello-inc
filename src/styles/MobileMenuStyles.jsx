const styles = (theme) => ({
  navItem: {
    textDecoration: 'none',
  },
  root: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
  navItems: {
    margin: '1rem 0 1rem 0',
  },
});

export default styles;
